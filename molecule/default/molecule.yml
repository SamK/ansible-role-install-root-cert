---
dependency:
  name: galaxy
driver:
  name: docker
lint:
  name: yamllint
platforms:
  - name: instance
    image: "${MOLECULE_DOCKER_IMAGE}"
provisioner:
  name: ansible
  lint:
    name: ansible-lint
    options:
      x:
        - 502
        - 504
verifier:
  name: testinfra
  lint:
    name: flake8
